# 登录与注册重构 Task

---

## 条目基本信息

- 条目 ID：AUTH-ITEM-001
- 条目标题：建立独立 `auth` 上下文核心模型与用例
- 所属需求：AUTH-REFactor-001
- 所属层：`domain / application`
- 优先级：高

---

## 1. 目标

建立独立 `auth` 上下文，承载注册与登录的核心认证语义，避免继续把认证逻辑堆在现有 `user` 示例模块中。

---

## 2. 输入

- 规格章节：`docs/specs/auth_refactor_spec.md`
- 依赖条目：无
- 外部约束：
  - 登录成功返回访问令牌
  - 认证上下文拆为独立 `auth`

---

## 3. 输出

- 代码产出：
  - `app/domain/auth/`
  - `app/application/auth/`
- 文档产出：
  - 当前任务文档
- 测试产出：
  - `auth` 领域与应用层测试

---

## 4. 实现边界

### 允许改动

- 新增 `auth` 领域对象、仓储端口、凭证服务端口、异常
- 新增注册与登录用例
- 对现有 `user` 模型做最小必要适配

### 禁止改动

- 直接在 `routes.py` 中写认证核心逻辑
- 直接让应用层依赖具体哈希/JWT 实现

---

## 5. 分层说明

- 当前条目归属原因：认证规则和用例属于核心层，必须先建模再接适配器
- 是否影响其他层：后续将驱动基础设施和接口层适配
- 是否新增依赖方向风险：否，前提是应用层仅依赖端口

---

## 6. 前置依赖

1. 规格已明确登录返回访问令牌
2. 已确认认证上下文独立为 `auth`

---

## 7. 执行门禁

### 7.1 Spec Gate

- 本条目是否已有足够规格支撑：是

### 7.2 Policy Gate

- 是否符合当前策略：是
- 是否存在违规风险：若将哈希/JWT 细节放入应用层则违规

### 7.3 Dependency Gate

- 前置依赖是否满足：是

### 7.4 Approval Gate

- 是否需要人工确认：否
- 确认点：无

### 7.5 Test Gate

- 需要新增哪些测试：
  - 注册用例
  - 登录用例
  - 认证失败路径
- 完成后要运行哪些验证：
  - `pytest`

---

## 8. 完成标准

1. `auth` 上下文已建立，核心规则不落在接口层和 ORM 中。
2. 注册与登录用例均只依赖领域端口。
3. 已为核心规则补充测试。

---

## 9. 执行记录

- 执行状态：`已完成`
- 阻塞原因：
- 备注：
  - 已建立独立 `auth` 上下文
  - 已补注册与登录用例及应用层测试

---

## 条目基本信息

- 条目 ID：AUTH-ITEM-002
- 条目标题：补齐认证基础设施、接口与迁移
- 所属需求：AUTH-REFactor-001
- 所属层：`infrastructure / interfaces / alembic`
- 优先级：高

---

## 1. 目标

为 `auth` 核心层接上持久化、密码哈希、访问令牌签发、HTTP API 与数据库迁移。

---

## 2. 输入

- 规格章节：`docs/specs/auth_refactor_spec.md`
- 依赖条目：`AUTH-ITEM-001`
- 外部约束：
  - 登录成功返回访问令牌
  - 表结构改动必须通过 Alembic

---

## 3. 输出

- 代码产出：
  - 认证相关仓储与服务实现
  - 登录/注册接口
  - 数据模型与迁移
- 文档产出：
  - 无
- 测试产出：
  - 接口或集成验证测试

---

## 4. 实现边界

### 允许改动

- 调整 `users` 表和 ORM Model
- 新增认证适配器与配置项
- 调整对外 API 暴露

### 禁止改动

- 直接把 JWT 细节暴露到领域层
- 跳过迁移只改 ORM

---

## 5. 分层说明

- 当前条目归属原因：本条目负责接入技术细节与 HTTP 适配器
- 是否影响其他层：会依赖核心层端口与用例
- 是否新增依赖方向风险：若接口层直接处理认证逻辑则有风险

---

## 6. 前置依赖

1. `AUTH-ITEM-001` 已完成
2. 核心模型与用例已稳定

---

## 7. 执行门禁

### 7.1 Spec Gate

- 本条目是否已有足够规格支撑：是

### 7.2 Policy Gate

- 是否符合当前策略：是
- 是否存在违规风险：若路由层直接持有会话与业务判断则违规

### 7.3 Dependency Gate

- 前置依赖是否满足：依赖 `AUTH-ITEM-001`

### 7.4 Approval Gate

- 是否需要人工确认：否
- 确认点：无

### 7.5 Test Gate

- 需要新增哪些测试：
  - 迁移后注册/登录关键路径
  - 失败路径响应
- 完成后要运行哪些验证：
  - `pytest`

---

## 8. 完成标准

1. 已通过迁移补齐认证所需存储结构。
2. 登录成功返回访问令牌。
3. 接口层仅做收参、调用、响应转换。

---

## 9. 执行记录

- 执行状态：`已完成`
- 阻塞原因：
- 备注：
  - 已补仓储、密码哈希、JWT 访问令牌签发、接口与迁移
  - 已执行 `pytest`
